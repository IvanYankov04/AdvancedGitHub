function joinArrayWithDelimiter(array, delimiter) {
    // Use the join method to concatenate the array elements with the delimiter
    const result = array.join(delimiter);
    
    // Print the result to the console
    console.log(result);
  }
joinArrayWithDelimiter(['One',

'Two',

'Three',

'Four',

'Five'],

'-')